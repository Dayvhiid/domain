{
  "name": "deploy-webhook",
  "version": "1.0.0",
  "description": "GitHub webhook receiver for pull-based deployment",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
